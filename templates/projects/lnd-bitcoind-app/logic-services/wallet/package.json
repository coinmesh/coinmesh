{
  "name": "@coinmesh/lnd-wallet",
  "version": "0.1.0",
  "scripts": {
    "start": "npm install && npm run rebuild-packages && concurrently \"nodemon ./bin/www\"",
    "rebuild-packages": "cd ./node_modules/ln-service && npm rebuild && cd -",
    "test": "nodemon --exec 'jasmine'"
  },
  "dependencies": {
    "@coinmesh/lnd-adapter": "0.1.0",
    "concurrently": "^3.6.1",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "express": "~4.15.5",
    "jasmine": "^3.2.0",
    "nodemon": "^1.18.3",
    "supertest": "^3.1.0"
  },
  "main": "index.js"
}
